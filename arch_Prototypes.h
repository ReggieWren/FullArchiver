// Function that creates an archive
void Archive(int numOfArgs, const char * fileArgs[]);

//Function to create multiple archives
void multiArchive(int numOfArgs, const char* fileArgs[]);

// Function that unpacks archive
void unArchive(int numOfArgs, const char * fileArgs[]);

