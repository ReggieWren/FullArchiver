// Function that prints stats of the archive
void listArchive(int numOfArgs, const char * fileNames[]);

// Function that checks the status of the archive
int checkArchive(int numOfArgs, const char * fileArgs[]);

// Function that checks for corruption of archive
int checkCorrupt(int arglength, const char * fileargs[]);
